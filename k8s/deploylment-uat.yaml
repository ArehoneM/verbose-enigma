apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-uat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app-uat
  template:
    metadata:
      labels:
        app: my-app-uat
    spec:
      containers:
      - name: my-app
        image: localhost:5000/demoapp:v1.0-uat
